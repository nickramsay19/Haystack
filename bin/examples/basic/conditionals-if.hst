push 1              ; push 1 to the top of the stack
maybe push 33       ; push '!' to the stack IF item at the top of the stack (1) is true, so yes
then print          ; if yes, print the item ('!')
